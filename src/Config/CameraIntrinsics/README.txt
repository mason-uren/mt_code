Camera intrinsics are placed under these directorys.

Each set should be in their own subdir, named after the date the calibration is done,
e.g. 2020-07-01. Each subdir also should have a README outlining the circumtance
of the image acquisition, and running the calibration code, along with pointers
to the image locations and reprojection errors.
